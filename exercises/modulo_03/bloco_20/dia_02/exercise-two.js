// USE sakila;
// SELECT city FROM city;
// SELECT first_name, last_name FROM customer;
// SELECT * FROM rental;
// SELECT title AS título, description AS descrição, release_year AS 'data de lançamento' FROM film;
// SELECT * FROM actor, address, category, city, country, customer, film, film_actor, film_category, film_text, filme, inventory, language, payment, rental, staff, store;